export default {
  theme: {
    extend: {
      colors: {
        olive: {
          DEFAULT: '#74714e', // now usable as `text-brand`, `bg-brand`, etc.
          dark: '#74714e',
        },
      },
    },
  },
};
